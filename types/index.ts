export type MenuItem = {
  id: number;
  label: string;
  href: string;
};

export type bioParagraph = {
  paragraph: string;
};

export type Experience = {
  period: string;
  role: string;
  company: string;
  description: string;
  tags: string[];
};
